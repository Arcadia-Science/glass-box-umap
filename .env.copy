# API token for the PyPI prod server. Passed explicitly via --token flag in Makefile.
UV_PUBLISH_TOKEN=

# API token for the PyPI test server. Passed explicitly via --token flag in Makefile.
UV_PUBLISH_TOKEN_TEST=
